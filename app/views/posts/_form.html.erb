<%= form_for @post do |f| %>
  <div class="form-control">
  	<%= f.label :title %>
  	<%= f.text_field :title %>
  </div>

  <div class="form-control">
  	<%= f.label :bodye %>
  	<%= f.text_field :body %>
  </div>

  <div class="form-control">
  	<%= f.submit 'Save' %>
  </div>
<% end %>