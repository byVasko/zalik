<h1><%= @post.title %></h1>
<p><%= @post.body %></p>
<p><%= image_tag @post.image.url(:thumb), class: 'img-show' if @post.image? %></p>

<%= link_to 'Edit', edit_post_path(@post) %>
<%= link_to 'Delete', post_path(@post), method: :delete, data: { confirm: 'Realy?'} %>